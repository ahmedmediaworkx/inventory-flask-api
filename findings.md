# Findings

- The repository initially contained only `AGENTS.md`; there is no legacy implementation or command configuration to preserve.
- The approved domain requires categories, products, stock adjustments, query/pagination, consistent errors, OpenAPI, migrations, tests, and Docker.
- Authentication is explicitly outside the project scope.
